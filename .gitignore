/target

# Local working files: session logs, #write dumps, captured play.
# Keep anything from a real game session out of a public repo.
*.local.tin
.judytinrc

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.gate.lock*
